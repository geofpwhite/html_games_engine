<!DOCTYPE html>
<html lang="en">

	<head>
		<link rel="icon" type="image/png" href="/games/favicon.png">
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Connect the Dots</title>
  <script type="text/javascript" >
  const url = "https://geof.geofpwhite.us"

  async function newgame(){
    let x = await fetch(url+"/games/connect-the-dots/new_game")
    let code = await x.text()
    code = code.substring(1,7)
    window.location.href = url+ "/games/connect-the-dots/" + code

  }

  function joingame(){
    const id = document.getElementById("gameID").value.trim()
    if (id) window.location.href = url + "/games/connect-the-dots/" + id
  }
  </script>
  {{ template "styles" . }}
	</head>

	<body>
		{{ template "navbar" . }}
		<div class="page-center">
			<div class="card">
				<h2>Connect the Dots</h2>
				<button class="button" onclick="newgame()">New Game</button>
				<br><br>
				<input type="text" id="gameID" placeholder="Game ID">
				<button class="button" onclick="joingame()">Join Game</button>
			</div>
		</div>
	</body>

</html>
